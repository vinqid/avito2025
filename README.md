# E2E Тесты Task Tracker - Инструкция

## 📋 Содержание проекта

Полный набор автоматизированных E2E тестов для приложения Task Tracker с использованием Playwright и TypeScript.

### Основные файлы:

**Тесты:**
- `autotests.spec.ts` - 5 полноценных E2E сценариев

**Документация:**
- `E2E-С-ДОСКОЙ.md` - Подробное описание каждого тест-кейса
- `ИНСТРУКЦИЯ.md` - Полная инструкция по установке и запуску
- `БЫСТРЫЙ_СТАРТ.md` - Быстрый старт за 5 минут

**Конфигурация:**
- `playwright.config.ts` - Конфигурация Playwright
- `tsconfig.json` - Конфигурация TypeScript
- `package.json` - Зависимости и npm скрипты

---

## ✅ Что покрывают тесты

### Функциональность:
- ✅ Создание задачи со всеми полями (Название, Описание, Проект, Приоритет, Исполнитель)
- ✅ Открытие карточки задачи с отображением всех данных
- ✅ Кнопка "Перейти на доску" в карточке
- ✅ Переход на доску проекта в отдельном окне
- ✅ Отображение задачи в правильной колонке доски
- ✅ Перемещение задач между статусами (drag-drop)
- ✅ Обновление и проверка статусов
- ✅ Редактирование данных задачи
- ✅ Работа с несколькими задачами одновременно

### Статусы:
- ✅ Backlog - устанавливается автоматически при создании (нельзя выбрать)
- ✅ InProgress - задача перемещается через drag-drop
- ✅ Done - финальный статус

### Компоненты:
- ✅ Главная страница со списком задач
- ✅ Модальное окно создания задачи
- ✅ Карточка задачи (просмотр и редактирование)
- ✅ Доска проекта (Kanban - три колонки)

---

## 🚀 Установка и запуск

### Требования:
- Node.js версия 16 и выше
- npm или yarn

### Шаг 1: Установка зависимостей

```bash
npm install
```

### Шаг 2: Установка браузеров Playwright

```bash
npx playwright install
```

### Шаг 3: Запуск тестов

```bash
# Все тесты
npm test autotests.spec.ts

# Конкретный тест
npm test -- -g "E2E-001"

# Конкретный тест с номером
npm test autotests.spec.ts -g "E2E-001"
```

---

## 📊 Описание E2E тестов

### E2E-001: Создание → Карточка → Переход на доску через кнопку

**Что тестируется:**
1. Создание новой задачи со всеми полями
2. Открытие карточки созданной задачи
3. Наличие кнопки "Перейти на доску"
4. Переход на доску проекта
5. Отображение задачи в колонке "To Do"
6. Статус задачи = Backlog

**Проверяемые элементы:**
- Форма создания со всеми полями
- Карточка с полной информацией
- Доска проекта с тремя колонками
- Позиция задачи в "To Do"

---

### E2E-002: Карточка → Доска → Перемещение → Проверка статуса

**Что тестируется:**
1. Создание задачи
2. Открытие карточки (проверка статуса = Backlog)
3. Переход на доску через кнопку
4. Перемещение задачи из "To Do" в "In Progress"
5. Обновление статуса в карточке

**Проверяемые элементы:**
- Переход между компонентами
- Drag-drop перемещение
- Обновление статуса после перемещения

---

### E2E-003: Карточка → Доска → Перемещение в Done напрямую

**Что тестируется:**
1. Создание задачи
2. Открытие карточки
3. Переход на доску
4. Перемещение из "To Do" прямо в "Done" (минуя InProgress)
5. Финальная проверка статуса = Done

**Проверяемые элементы:**
- Возможность прямого перемещения
- Верный финальный статус

---

### E2E-004: Редактирование → Карточка → Доска → Проверка

**Что тестируется:**
1. Создание задачи
2. Открытие и редактирование карточки (изменение названия)
3. Сохранение изменений
4. Переход на доску
5. Проверка что обновленное название видно на доске

**Проверяемые элементы:**
- Функция редактирования
- Сохранение данных
- Отображение обновленных данных на доске

---

### E2E-005: Три задачи → Открыть каждую → Доска → Проверить позицию

**Что тестируется:**
1. Создание трех задач
2. Открытие первой задачи и переход на доску
3. Отображение всех трех задач на доске
4. Перемещение: 1-я в "InProgress", 2-я в "Done", 3-я остается в "To Do"
5. Проверка позиций и статусов всех трех

**Проверяемые элементы:**
- Работа с несколькими задачами
- Корректная позиция каждой задачи
- Правильные статусы после перемещения

---

## 🎯 Используемые селекторы

Тесты используют гибкие селекторы, которые работают с различными реализациями UI:

```typescript
// Кнопки
button:has-text("СОЗДАТЬ ЗАДАЧУ")
button:has-text("Перейти на доску")

// Поля ввода
input[placeholder*="Название"]
textarea[placeholder*="Описание"]

// Dropdown селекты
[role="combobox"], select

// Текстовые элементы
text=/Проект|Приоритет|Исполнитель/i

// Колонки доски
text=To Do, text=In Progress, text=Done
```

---

## ⚙️ Конфигурация

### playwright.config.ts
Содержит:
- Поддержка трех браузеров: Chromium, Firefox, WebKit
- Базовый URL приложения
- HTML отчеты и скриншоты при ошибках
- Таймауты и повторы

### package.json
Npm скрипты:
```json
{
  "test": "playwright test",
  "test:ui": "playwright test --ui",
  "test:debug": "playwright test --debug",
  "test:headed": "playwright test --headed",
  "test:chrome": "playwright test --project=chromium",
  "test:firefox": "playwright test --project=firefox",
  "test:webkit": "playwright test --project=webkit",
  "report": "playwright show-report",
  "codegen": "playwright codegen https://avito-tech-internship-psi.vercel.app/"
}
```

---

## 🛠️ Команды для разработки

### Интерактивные режимы:

```bash
# UI режим (интерактивный интерфейс)
npm run test:ui

# Debug режим (пошаговое выполнение)
npm run test:debug

# Видимый браузер
npm run test:headed

# Генерация селекторов
npm run codegen
```

### Выбор браузера:

```bash
npm run test:chrome
npm run test:firefox
npm run test:webkit
```

### Просмотр отчета:

```bash
npm run report
```

---

## 📝 Логика тестов

### Базовая структура теста:

1. **Предусловие:** Загрузка приложения
2. **Создание:** Создание задачи со всеми полями
3. **Действие:** Открытие карточки или доски
4. **Проверка:** Verify что нужные элементы видны
5. **Операция:** Перемещение или редактирование
6. **Финальная проверка:** Verify итогового состояния

### Паттерны ожидания:

```typescript
// Ожидание загрузки страницы
await page.waitForLoadState('networkidle');

// Ожидание после действия
await page.waitForTimeout(1000);

// Ожидание видимости элемента
await expect(element).toBeVisible();
```

---

## ⚠️ Важные моменты

### Статусы задач:
- При создании статус = **Backlog** (автоматически, не выбирается)
- На доске "To Do" = Backlog
- Статус меняется только через drag-drop на доске

### Форма создания:
- Все поля обязательны (Название, Проект, Приоритет, Исполнитель)
- Описание - опциональное
- Статус не выбирается

### Доска проекта:
- Три колонки: To Do, In Progress, Done
- Drag-drop для перемещения
- Статус обновляется автоматически

### Карточка задачи:
- Содержит кнопку "Перейти на доску"
- При редактировании показывает все поля
- Статус Backlog нельзя менять в карточке

---

## 🐛 Отладка

### Если тесты не находят элементы:

1. Запустите codegen для получения правильных селекторов:
```bash
npm run codegen
```

2. Обновите селекторы в файле теста

3. Перезапустите тест

### Если элементы находятся с задержкой:

1. Увеличьте timeout в платвье конфиге
2. Добавьте дополнительные page.waitForTimeout()
3. Используйте page.waitForLoadState()

### Просмотр what went wrong:

```bash
npm run report
```

Откроется HTML отчет с скриншотами и видео ошибок

---

## 📈 Структура проекта

```
project/
├── autotests.spec.ts   # Основные тесты
├── E2E-С-ДОСКОЙ.md                    # Документация тестов
├── ИНСТРУКЦИЯ.md                      # Полная инструкция
├── БЫСТРЫЙ_СТАРТ.md                  # Быстрый старт
├── playwright.config.ts               # Конфиг Playwright
├── tsconfig.json                      # Конфиг TypeScript
├── package.json                       # Зависимости
├── node_modules/                      # Зависимости (после npm install)
└── playwright-report/                 # Отчеты (после запуска)
```

---

## ✅ Чек-лист перед запуском

- [ ] Node.js версия 16+ установлена
- [ ] `npm install` выполнена
- [ ] `npx playwright install` выполнена
- [ ] Приложение доступно по адресу: https://avito-tech-internship-psi.vercel.app/
- [ ] Интернет соединение активно

---

## 🎓 Примеры использования

### Запустить один конкретный тест:

```bash
npm test -- -g "E2E-001"
```

### Запустить тесты содержащие "Перемещение":

```bash
npm test -- -g "Перемещение"
```

### Запустить с подробным логированием:

```bash
npm test -- --verbose
```

### Запустить и оставить браузер открытым:

```bash
npm run test:headed
```

---

## 📞 Поддержка

### Частые проблемы:

**Проблема:** Тесты не запускаются  
**Решение:** Проверьте что выполнили `npm install` и `npx playwright install`

**Проблема:** Селекторы не найдены  
**Решение:** Используйте `npm run codegen` для генерации селекторов

**Проблема:** Timeout при выполнении  
**Решение:** Увеличьте timeout в playwright.config.ts

**Проблема:** Приложение недоступно  
**Решение:** Проверьте что URL: https://avito-tech-internship-psi.vercel.app/ доступен

---

## 🚀 Как все работает

1. Тест загружает приложение
2. Создает задачу заполняя форму
3. Открывает карточку созданной задачи
4. Проверяет наличие кнопки "Перейти на доску"
5. Переходит на доску проекта
6. Проверяет что задача видна в правильной колонке
7. Перемещает задачу между статусами (если нужно)
8. Проверяет что статус обновился
9. Открывает задачу и проверяет что статус актуален

**Всё это происходит автоматически без участия человека!**

---

**Готово к запуску!** 🎉
